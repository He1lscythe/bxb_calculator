# ── Python cache ──────────────────────────────────────────
__pycache__/

# ── Node modules ──────────────────────────────────────────
node_modules/

# ── Crawl progress (local state) ──────────────────────────
progress.json
soul_progress.json
# 历史上爬虫会同时输出 *.js 副本（var XXX_DATA = ...）。前端不再依赖，
# 爬虫已不生成；此规则作为保险，防止误生成文件被入库。
data/*.js

# ── Lookup / intermediate files (large altema scrapes + lookup tables) ──
_lookup/

# ── revise / extra: data-staging 上 tracked (本 branch 不 ignore)、main 上 ignored ──
# main / GitHub Pages 上 fetch 404 容错为 [] / {}。
*_extra.json
data/omoide_templates.json

# ── Local-only progress markers (per-item check state, never published) ──
data/souls_check.json
data/characters_check.json
data/crystals_check.json
data/bladegraphs_check.json

# ── crawler schema 自检报告（每次 crawl 重生、不入 git） ──
data/crawl_warnings_*.json

# icons/ 跟 data/omoide/ 在 2026-06-09 改为入 git (单 worktree 架构、main + data-staging 共享 tracking)

# ── Docs not for Pages: 只 commit chara_training / structure / senzai_icon_table ──
docs/checklist.md
docs/frontend_ui.md
docs/hensei.md
docs/motion_table.md
docs/skills_schema.md
docs/*_doc_cn.md
docs/draft_doc_cn.md

# ── Audit one-time analysis (gitignored, local only) ──────
audit/

# ── Hidden temp / partial JSON ────────────────────────────
_*.json
# v2 refactor: 一次性 wiki 补全字段提取产物、需要入 git
!data/_wiki_aux.json
!data/_wiki_unmatched_crystals.json
!data/_audit_crystals_null_max.json
!data/_audit_crystals_null_math.json
data/soul_affinity_progress.txt
draft/

# ── Claude local settings ─────────────────────────────────
.claude/

# ── Playwright artifacts ──────────────────────────────────
test-results/
playwright-report/
playwright/.cache/

# ── 临时 main branch 对比 (Phase 7 调研用、不入 git) ──────
_tmp_main_full/
