{
  "name": "bxb-dispatch",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: 可靠定时触发 GitHub Actions (workflow_dispatch)",
  "scripts": {
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev",
    "tail": "npx wrangler tail"
  }
}
